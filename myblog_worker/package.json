{
    "name": "myblog-worker",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "db:init": "wrangler d1 execute blog-db --local --file=./schema.sql",
        "db:init:remote": "wrangler d1 execute blog-db --remote --file=./schema.sql"
    },
    "dependencies": {
        "hono": "^4.0.0"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}